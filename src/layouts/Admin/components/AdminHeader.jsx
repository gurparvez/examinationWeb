import React from 'react';

const AdminHeader = ({ name, role, department, program, classname, props }) => {
    console.log(name, role, department, program, classname);
    return <div>AdminHeader</div>;
};

export default AdminHeader;
