// api call to admin login

import { useMutation } from '@tanstack/react-query';

// TODO: complete this
const useAdminLogin = () => {
    return useMutation({
        mutationFn: ({}) => {},
        onSuccess: (data) => {},
        onError: (err) => {},
    })
}

export default useAdminLogin;